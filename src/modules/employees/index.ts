// Employees module — owns the employee directory (create/update/delete, credentials, holiday
// balances). Distinct from the Auth module: this is about managing employee records, not
// verifying who's logged in.

export { EmployeesClient } from './components/EmployeesClient'
