// Leaderboard module — the Stats & Leaderboard page. Depends on the Leave & Attendance module's
// monthly leave summary (holidays/sick/sudden-leave/late-arrival counts feed the score penalty).

export { StatsClient } from './components/StatsClient'
