// Dashboard module — the landing-page overview. This is an aggregator that composes data owned
// by Timetable, Leave & Attendance, and Shift Management into one summary view (analogous to a
// "backend for frontend" in a real service split), rather than owning any domain data itself.

export { DashboardClient } from './components/DashboardClient'
